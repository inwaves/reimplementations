import LSTM

def generate_sequences(model, word, seq_length):
    """Sample a given number of tokens from the distribution that this model outputs
       given input word.

    Args:
        model (LSTM): An LSTM model.
        word (str): A word to start the sequence with.
        seq_length (int): The length of the sequence.
    """
    pass

if __name__ == "__main__":
    # initialise an LSTM
    # train it
    # pass it to the generate_sequences function
    pass